<h2>Here is a dandy list of all the stores currently in the database:</h2>

<p>Click on a store to view its detail page along with the brands it carries.</p>

<% if @stores.any? %>
  <ul>
    <% @stores.each do |store| %>
      <li><a href="/stores/<%= store.id %>"><%= store.name %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>There are currently no stores in the database!</p>
<% end %>
